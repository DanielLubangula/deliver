<style>
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #1e2a3a;
        color: #ffffff;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
    }

    .register-container {
        max-width: 800px;
        margin: 2rem auto;
        padding: 0 1rem;
    }

    .register-card {
        background-color: #34495e;
        border-radius: 10px;
        padding: 2rem;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .register-title {
        text-align: center;
        color: #5ce1e6;
        margin-bottom: 2rem;
        font-size: 1.5rem;
    }

    .form-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 1.5rem;
    }

    @media (max-width: 768px) {
        .form-grid {
            grid-template-columns: 1fr;
        }
    }

    .form-group {
        margin-bottom: 1.5rem;
    }

    .form-section-title {
        color: #5ce1e6;
        margin: 2rem 0 1rem;
        grid-column: 1 / -1;
        border-bottom: 1px solid #5ce1e6;
        padding-bottom: 0.5rem;
    }

    .form-label {
        display: block;
        margin-bottom: 0.5rem;
        color: #a8b2bc;
    }

    .form-input {
        width: 100%;
        padding: 0.75rem;
        border: 1px solid #5ce1e6;
        border-radius: 5px;
        background-color: #2c3e50;
        color: #ffffff;
        box-sizing: border-box;
    }

    .form-input::placeholder {
        color: #a8b2bc;
    }

    .image-preview-container {
        grid-column: 1 / -1;
        text-align: center;
        margin-bottom: 1.5rem;
    }

    .image-preview {
        max-width: 200px;
        max-height: 200px;
        margin: 1rem auto;
        border-radius: 10px;
        display: none;
        border: 2px solid #5ce1e6;
    }

    .image-upload {
        display: none;
    }

    .image-upload-label {
        display: inline-block;
        padding: 0.75rem 1.5rem;
        background-color: #5ce1e6;
        color: #1e2a3a;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

    .image-upload-label:hover {
        background-color: #4bb8bd;
    }

    .terms-container {
        grid-column: 1 / -1;
        margin: 1rem 0;
    }

    .terms-checkbox {
        display: flex;
        align-items: flex-start;
        gap: 0.5rem;
        margin-bottom: 1rem;
    }

    .terms-checkbox input[type="checkbox"] {
        margin-top: 0.25rem;
        accent-color: #5ce1e6;
    }

    .register-button {
        width: 100%;
        background-color: #5ce1e6;
        color: #1e2a3a;
        border: none;
        padding: 1rem;
        border-radius: 5px;
        cursor: pointer;
        font-weight: bold;
        font-size: 1rem;
        transition: background-color 0.3s ease;
        grid-column: 1 / -1;
    }

    .register-button:hover {
        background-color: #4bb8bd;
    }

    .form-footer {
        text-align: center;
        margin-top: 1.5rem;
        grid-column: 1 / -1;
    }

    .form-footer a {
        color: #5ce1e6;
        text-decoration: none;
    }

    .form-footer a:hover {
        text-decoration: underline;
    }
    /* Terme et condition*/
     .terms-links {
        color: #5ce1e6;
        text-decoration: none;
        transition: color 0.3s ease;
    }

    .terms-links:hover {
        color: #4bb8bd;
        text-decoration: underline;
    }

    .modal {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.7);
        z-index: 1000;
    }

    .modal-content {
        position: relative;
        background-color: #34495e;
        margin: 5% auto;
        padding: 2rem;
        width: 80%;
        max-width: 800px;
        max-height: 80vh;
        overflow-y: auto;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .close-modal {
        position: absolute;
        right: 1rem;
        top: 1rem;
        font-size: 1.5rem;
        color: #5ce1e6;
        cursor: pointer;
        transition: color 0.3s ease;
    }

    .close-modal:hover {
        color: #4bb8bd;
    }

    .modal-title {
        color: #5ce1e6;
        margin-bottom: 1.5rem;
    }

    .modal-section {
        margin-bottom: 1.5rem;
    }

    .modal-section h3 {
        color: #5ce1e6;
        margin-bottom: 1rem;
    }

    .modal-section p {
        color: #ffffff;
        line-height: 1.6;
        margin-bottom: 1rem;
    }
    .msgerr{
        text-align: center;
        color: rgb(190, 91, 91);
        text-shadow: 0px 0px 1px rgb(5, 5, 5);
    }
    /* Terme et condition*/
</style>
</head>
<body>

    <div class="register-container">
        <div class="register-card">
            <h2 class="register-title">Inscription Vendeur</h2>

            <form id="sellerRegisterForm" action="/deliver/registerGestion" method="POST" enctype="multipart/form-data">
                <div class="form-grid">
                    <h3 class="form-section-title">Informations de l'entreprise</h3>
                    
                    <div class="form-group">
                        <label class="form-label">Nom de l'entreprise</label>
                        <input type="text" class="form-input" value="{{msgErrorInscVendor.companyName}}" name="companyName" required>
                    </div>

                    <div class="form-group">
                        <label class="form-label">Adresse de l'entreprise</label>
                        <input type="text" class="form-input" value="{{msgErrorInscVendor.companyAddress}}" name="companyAddress" required>
                    </div>

                    <div class="form-group">
                        <label class="form-label">Téléphone professionnel</label>
                        <input type="tel" class="form-input" value="{{msgErrorInscVendor.phone}}" name="phone" required>
                    </div>

                    <h3 class="form-section-title">Informations de connexion</h3>

                    <div class="form-group">
                        <label class="form-label">Email professionnel</label>
                        <input type="email" class="form-input" value="{{msgErrorInscVendor.email}}" name="email" required>
                    </div>

                    <div class="form-group">
                        <label class="form-label">Mot de passe</label>
                        <input type="password" class="form-input" value="{{msgErrorInscVendor.password}}" name="password" required>
                    </div>

                    <div class="form-group">
                        <label class="form-label">Confirmer le mot de passe</label>
                        <input type="password" class="form-input" value="{{msgErrorInscVendor.confirmPassword}}" name="confirmPassword" required>
                    </div>

                    <div class="image-preview-container">
                        <label class="form-label">Logo de l'entreprise</label>
                        <img id="preview" class="image-preview" alt="Prévisualisation du logo">
                        <input type="file" id="imageUpload" name="companyLogo" class="image-upload" accept="image/*">
                        <label for="imageUpload" class="image-upload-label">Choisir une image</label>
                    </div>

                    <div class="terms-container">
                          <!-- ... [Previous HTML until terms-checkbox] ... -->
    <div class="terms-container">
        <div class="terms-checkbox">
            <input type="checkbox" id="terms" name="terms" required>
            <label for="terms">J'accepte les <a href="#" class="terms-links" onclick="showTerms()">conditions générales d'utilisation</a> et la <a href="#" class="terms-links" onclick="showPrivacy()">politique de confidentialité</a></label>
        </div>
        <div class="terms-checkbox">
            <input type="checkbox" id="newsletter" name="newsletter">
            <label for="newsletter">Je souhaite recevoir les actualités et offres commerciales</label>
        </div>
    </div>
    <p class="msgerr">
        {{msgErrorInscVendor.msgErr}}
    </p>
    <!-- Modal for Terms -->
    <div id="termsModal" class="modal">
        <div class="modal-content">
            <span class="close-modal" onclick="closeTerms()">&times;</span>
            <h2 class="modal-title">Conditions Générales d'Utilisation</h2>
            <div class="modal-section">
                <h3>1. Objet</h3>
                <p>Les présentes Conditions Générales d'Utilisation (CGU) régissent l'utilisation de la plateforme DeliverEase par les vendeurs professionnels. En vous inscrivant sur notre plateforme, vous acceptez sans réserve l'ensemble de ces conditions.</p>
            </div>
            <div class="modal-section">
                <h3>2. Inscription et Compte Vendeur</h3>
                <p>Pour utiliser nos services, vous devez créer un compte vendeur en fournissant des informations exactes et à jour. Vous êtes responsable de la confidentialité de vos identifiants de connexion.</p>
            </div>
            <div class="modal-section">
                <h3>3. Obligations du Vendeur</h3>
                <p>En tant que vendeur, vous vous engagez à :
                   - Fournir des informations exactes sur votre entreprise
                   - Respecter les délais de livraison
                   - Maintenir une qualité de service conforme à nos standards
                   - Respecter la législation en vigueur</p>
            </div>
            <div class="modal-section">
                <h3>4. Commission et Paiements</h3>
                <p>DeliverEase prélève une commission sur chaque vente réalisée via la plateforme. Les modalités de paiement et les taux de commission sont détaillés dans votre espace vendeur.</p>
            </div>
        </div>
    </div>

    <!-- Modal for Privacy Policy -->
    <div id="privacyModal" class="modal">
        <div class="modal-content">
            <span class="close-modal" onclick="closePrivacy()">&times;</span>
            <h2 class="modal-title">Politique de Confidentialité</h2>
            <div class="modal-section">
                <h3>1. Collecte des Données</h3>
                <p>Nous collectons les informations nécessaires à la création et à la gestion de votre compte vendeur, incluant les coordonnées professionnelles et les informations de paiement.</p>
            </div>
            <div class="modal-section">
                <h3>2. Utilisation des Données</h3>
                <p>Les données collectées sont utilisées pour :
                   - La gestion de votre compte
                   - Le traitement des commandes
                   - L'amélioration de nos services
                   - La communication avec vous concernant votre activité</p>
            </div>
            <div class="modal-section">
                <h3>3. Protection des Données</h3>
                <p>Nous mettons en œuvre des mesures de sécurité appropriées pour protéger vos données contre tout accès non autorisé, modification, divulgation ou destruction.</p>
            </div>
            <div class="modal-section">
                <h3>4. Vos Droits</h3>
                <p>Conformément au RGPD, vous disposez d'un droit d'accès, de rectification, de suppression et de portabilité de vos données personnelles.</p>
            </div>
        </div>
    </div>

                    </div>

                    <button type="submit" class="register-button">Créer mon compte vendeur</button>

                    <div class="form-footer">
                        Déjà inscrit ? <a href="/deliver/loginVendor">Se connecter</a>
                    </div>
                </div>
            </form>
        </div>
    </div>

<script>
//Terme
// Previous image preview script remains the same

// Modal functions
function showTerms() {
            event.preventDefault();
            document.getElementById('termsModal').style.display = 'block';
        }

function showPrivacy() {
            event.preventDefault();
            document.getElementById('privacyModal').style.display = 'block';
        }

function closeTerms() {
            document.getElementById('termsModal').style.display = 'none';
        }

function closePrivacy() {
            document.getElementById('privacyModal').style.display = 'none';
        }

// Close modals when clicking outside
window.onclick = function(event) {
            if (event.target.className === 'modal') {
                event.target.style.display = 'none';
            }
        }

// Close modals with Escape key
document.addEventListener('keydown', function(event) {
            if (event.key === 'Escape') {
                document.getElementById('termsModal').style.display = 'none';
                document.getElementById('privacyModal').style.display = 'none';
            }
        });
//Terme
const imageUpload = document.getElementById('imageUpload');
const preview = document.getElementById('preview');
imageUpload.addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    preview.style.display = 'block';
                    preview.src = e.target.result;
                }
                reader.readAsDataURL(file);
            }
        });
</script>